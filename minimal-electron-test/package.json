{
  "name": "minimal-electron-test",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build:main": "tsc main.ts --outDir dist --module commonjs --esModuleInterop true",
    "build:preload": "tsc preload.ts --outDir dist --module commonjs --esModuleInterop true",
    "build": "npm run build:main && npm run build:preload",
    "start": "npm run build && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.0.0",
    "typescript": "^5.8.3"
  }
} 